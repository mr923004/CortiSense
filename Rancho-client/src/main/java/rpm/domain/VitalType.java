package rpm.domain;

// Types of vitals that are simulated and monitored in the system
public enum VitalType {
    HEART_RATE,
    RESP_RATE,
    BP_SYSTOLIC,
    BP_DIASTOLIC,
    TEMPERATURE,
    ECG
}
